# name: CI-CD with ECR

# on:
#   push:
#     branches:
#       - main

# env:
#   AWS_REGION: us-east-1
#   ECR_REPOSITORY: deerajaws/cicd-node-app

# jobs:

#   build-and-push:

#     runs-on: ubuntu-latest

#     steps:

#     - name: Checkout code
#       uses: actions/checkout@v3

#     - name: Configure AWS credentials
#       uses: aws-actions/configure-aws-credentials@v4
#       with:
#         aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#         aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#         aws-region: us-east-1

#     - name: Login to ECR
#       id: login-ecr
#       uses: aws-actions/amazon-ecr-login@v2

#     - name: Build and Push Image
#       run: |
#         IMAGE_URI=${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:latest
        
#         echo "Building image:"
#         echo $IMAGE_URI
        
#         docker build -t $IMAGE_URI .
        
#         docker push $IMAGE_URI


#   deploy:

#     needs: build-and-push

#     runs-on: ubuntu-latest

#     steps:

#     - name: Configure AWS credentials
#       uses: aws-actions/configure-aws-credentials@v4
#       with:
#         aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#         aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#         aws-region: us-east-1

#     - name: Login to ECR
#       id: login-ecr
#       uses: aws-actions/amazon-ecr-login@v2

#     - name: Deploy to EC2
#       uses: appleboy/ssh-action@v1.0.3
#       with:
#         host: ${{ secrets.EC2_HOST }}
#         username: ec2-user
#         key: ${{ secrets.EC2_SSH_KEY }}
#         script: |

#           IMAGE_URI=${{ steps.login-ecr.outputs.registry }}/deerajaws/cicd-node-app:latest

#           echo "Deploying image:"
#           echo $IMAGE_URI

#           aws ecr get-login-password --region us-east-1 \
#           | docker login \
#           --username AWS \
#           --password-stdin $(echo $IMAGE_URI | cut -d'/' -f1)

#           docker stop cicd-node-app || true
#           docker rm cicd-node-app || true

#           docker pull $IMAGE_URI

#           docker run -d \
#           -p 3000:3000 \
#           --name cicd-node-app \
#           --restart always \
#           $IMAGE_URI

# Without deploy, only push to ECR

name: CI-CD with ECR

on:
  push:
    branches:
      - main

env:
  AWS_REGION: us-east-1
  ECR_REPOSITORY: deerajaws/cicd-node-app

jobs:

  build-and-push:

    runs-on: ubuntu-latest

    steps:

    - name: Checkout code
      uses: actions/checkout@v3

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1

    - name: Login to ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v2

    - name: Build image
      run: |
        IMAGE_URI=${{ steps.login-ecr.outputs.registry }}/deerajaws/cicd-node-app:latest
        
        docker build -t $IMAGE_URI .
        
        docker push $IMAGE_URI